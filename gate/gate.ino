#define DECODE_FAST

#include <IRremote.hpp>

void setup() {
    Serial.begin(115200);

    //IrReceiver.begin(IR_RECEIVE_PIN, ENABLE_LED_FEEDBACK);
}

void loop() {
    //IrReceiver.RECEIVE???();

    //if (IrReceiver.decodedIRData.command == STARTSTOP;){
        //doTheThing();
   //}
}